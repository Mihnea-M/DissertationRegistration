{
    "name": "dissertation-registration",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "start": "npm run build:client && npm run start:server",
        "start:server": "npm --prefix DissertationRegistrationBackend run prod",
        "build:client": "npm --prefix DissertationRegistrationClient run build",
        "install:all": "npm install && npm --prefix DissertationRegistrationBackend install && npm --prefix DissertationRegistrationClient install",
        "test": "npm --prefix DissertationRegistrationBackend test"
    },
    "dependencies": {
        "dissertation-registration": "file:"
    }
}
